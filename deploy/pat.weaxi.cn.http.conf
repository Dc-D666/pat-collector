# 临时 HTTP 段：certbot 签发证书前使用（acme-challenge + 跳转 HTTPS）
server {
    listen 80;
    listen [::]:80;
    server_name pat.weaxi.cn;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
